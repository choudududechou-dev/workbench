# WPS 多维表格搭建教程 — 考勤 + 项目管理 + 年度汇总

> 适用：WPS Office 客户端（Windows/Mac）+ WPS 手机 App
> 难度：新手友好，每一步都有截图级文字说明

---

## 整体架构

用 **三张数据表 + 一个仪表盘** 覆盖你的全部需求：

```
考勤记录表          项目总表          项目收支明细表
(打卡+工时)    ←──→  (报价+状态)  ←──  (成本报销+收入)
                         │
                   月度/年度汇总仪表盘
                   (自动统计报表)
```

三张表之间用 **关联字段** 串起来，仪表盘自动拉数据出图表。

---

## 第一步：创建空白多维表格

### PC 端操作

1. 打开 **WPS Office**（桌面客户端）
2. 点击最左侧导航栏的 **新建**
3. 在搜索框输入 **多维表格**，或者直接找「多维表格」入口
4. 点击 **空白多维表格**
5. 顶部文件名改为：**考勤与项目管理**

就进入了一个全新的多维表格工作区。

### 手机端操作

WPS 手机 App → 底部 **「+」** → **多维表格** → **空白创建**。

> 手机也能创建，但**第一步建议在电脑上做**（建字段、设公式方便），之后日常打卡和录入在手机上做。

---

## 第二步：建第一张表 —— 考勤记录表

左侧新建的多维表格默认有一张空的「数据表1」，把它改成考勤记录表。

### 2.1 重命名表

双击左下角的「数据表1」，改名为 **考勤记录**

### 2.2 添加字段（列）

默认第一列是「文本」类型，我们在后面继续添加列。点击表格右上角的 **「+」** 加号按钮，逐一添加以下字段：

| 序号 | 字段名 | 字段类型 | 说明 |
|:---:|--------|:--------:|------|
| 1 | 姓名 | **文本** | 默认第一列改个名就行 |
| 2 | 打卡日期 | **日期** | 选「日期」类型，格式 YYYY-MM-DD |
| 3 | 签到时间 | **日期时间** | 注意选「日期时间」不是「日期」，包含时分秒 |
| 4 | 签退时间 | **日期时间** | 同上 |
| 5 | 打卡类型 | **单选** | 选项：外勤、内勤 |
| 6 | 外勤地点 | **文本** | 外勤打卡时填具体地址 |
| 7 | 工作时长 | **公式** | 自动算工时，见下方公式 |
| 8 | 备注 | **文本** | 可选 |

### 2.3 工作时长公式

在「工作时长」字段的公式编辑框中输入：

```excel
IF(AND({签到时间} != "", {签退时间} != ""), ROUND(({签退时间} - {签到时间}) * 24, 1), "")
```

**公式含义**：只有当签到和签退时间都填写了，才算工时，结果四舍五入保留 1 位小数（单位：小时）。

> 💡 如果公式不生效，试试简化版：
> ```
> ({签退时间} - {签到时间}) * 24
> ```

### 2.4 创建日历视图（方便看每天打卡情况）

1. 点击表格上方的 **「+ 添加视图」**
2. 选择 **日历视图**
3. 日期字段选「打卡日期」
4. 视图名改为 **打卡日历**

这样就能在日历上直观看到每个人的打卡记录。

---

## 第三步：建第二张表 —— 项目总表

点击左下角的 **「+」** 新建数据表，命名为 **项目总表**。

### 3.1 添加字段

| 序号 | 字段名 | 字段类型 | 说明 |
|:---:|--------|:--------:|------|
| 1 | 项目名称 | **文本** | 默认列，作为主标识 |
| 2 | 客户名称 | **文本** | 哪个客户的 |
| 3 | 报价金额 | **数字** | 格式选「货币」 |
| 4 | 项目状态 | **单选** | 选项：🟡跟进中、🟢已签约、🔵进行中、✅已完成 |
| 5 | 开始日期 | **日期** | |
| 6 | 预计结束日期 | **日期** | |
| 7 | 备注 | **文本** | |

### 3.2 按状态分组（看板视图）

1. 点击 **「+ 添加视图」**
2. 选择 **看板视图**
3. 分组字段选「项目状态」
4. 视图名改为 **项目看板**

现在你可以像拖卡片一样，把项目从「跟进中」拖到「已签约」再拖到「已完成」。

---

## 第四步：建第三张表 —— 项目收支明细

这是最关键的一张表，把所有收入和成本都记在这里，后续汇总全靠它。

点击左下角的 **「+」** 新建数据表，命名为 **项目收支明细**。

### 4.1 添加字段

| 序号 | 字段名 | 字段类型 | 说明 |
|:---:|--------|:--------:|------|
| 1 | 标题 | **文本** | 默认列，写简要说明如"XX项目材料费" |
| 2 | **关联项目** | **关联** ⚠️ | 关键字段！链接到「项目总表」 |
| 3 | 收支类型 | **单选** | 选项：💰收入、💸成本报销 |
| 4 | 金额 | **数字** | 格式选「货币」 |
| 5 | 日期 | **日期** | 费用发生的日期 |
| 6 | 费用类别 | **单选** | 选项：材料费、人工费、差旅费、交通费、招待费、办公费、其他 |
| 7 | 备注 | **文本** | 可选 |
| 8 | 凭证 | **附件** | 发票照片、报销单截图 |

### 4.2 💡 关联字段设置（重点）

1. 添加字段时选 **「关联」** 类型
2. 在弹出的列表中选中 **「项目总表」**
3. 确定后，这个字段就和项目总表连上了
4. 点击这个字段的单元格，会弹出项目总表的记录列表，勾选对应项目即可

### 4.3 添加「报价金额」引用

关联之后，我们还需要把项目总表的报价金额拉过来，方便后面算利润。

1. 添加一个新字段，字段名：**项目报价**
2. 字段类型选 **「引用」**（有些版本叫「查找引用」）
3. 引用的来源选：关联字段 → **项目总表.报价金额**
4. 这样每条收支记录会自动显示对应项目的报价

### 4.4 按月份分组视图

1. 点击 **「+ 添加视图」**
2. 选表格视图
3. 点击顶部的 **「分组」** 按钮
4. 分组字段选「日期」，分组成员选「按月」
5. 视图名改为 **按月汇总**

这样每月的收入和成本一目了然。

---

## 第五步：创建汇总仪表盘

这是**年/月度汇总**的核心，不需要写公式，点几下就能出图。

### 5.1 新建仪表盘

点击左下角的 **「+」** → 选择 **「仪表盘」**（有的版本叫「统计」或「数据分析」）。

### 5.2 添加统计卡片

在仪表盘空白处，添加以下组件：

#### 📊 卡片 1：本月总工时

| 设置项 | 值 |
|--------|-----|
| 数据来源 | 考勤记录 |
| 图表类型 | **统计数字** |
| 统计字段 | 工作时长 |
| 统计方式 | 求和 |
| 筛选条件 | 打卡日期 = 本月 |

#### 📊 卡片 2：本月总成本

| 设置项 | 值 |
|--------|-----|
| 数据来源 | 项目收支明细 |
| 图表类型 | **统计数字** |
| 统计字段 | 金额 |
| 统计方式 | 求和 |
| 筛选条件 | 收支类型 = 💸成本报销；日期 = 本月 |

#### 📊 卡片 3：本月总收入

同上，筛选条件改为「收支类型 = 💰收入」。

#### 📊 卡片 4：本月利润

用仪表盘的 **计算字段**：总收入 - 总成本（或在仪表盘里创建计算指标）。

#### 📈 卡片 5：项目利润排行（柱状图）

| 设置项 | 值 |
|--------|-----|
| 数据来源 | 项目收支明细 |
| 图表类型 | **柱状图** |
| 横轴（X） | 关联项目（项目名称） |
| 纵轴（Y） | 金额（求和） |
| 分组 | 收支类型 |
| 筛选 | 日期 = 今年 |

每个项目的收入和成本并排显示，利润一目了然。

#### 📈 卡片 6：月度趋势（折线图）

| 设置项 | 值 |
|--------|-----|
| 数据来源 | 项目收支明细 |
| 图表类型 | **折线图** |
| 横轴（X） | 日期（按月） |
| 纵轴（Y） | 金额（求和） |
| 分组 | 收支类型 |

#### 📋 卡片 7：年度项目汇总表（明细表）

| 设置项 | 值 |
|--------|-----|
| 数据来源 | 项目收支明细 |
| 图表类型 | **表格/明细** |
| 行分组 | 关联项目（项目名称） |
| 列汇总 | 金额（求和）、金额（收支类型 = 收入）、金额（收支类型 = 成本报销） |
| 计算 | 报价 - 成本 = 利润 |

---

## 第六步：设置手机快捷操作

### 6.1 手机端打卡

1. 手机打开 WPS App
2. 在「文档」中找到「考勤与项目管理」多维表格
3. 点进去，切换到 **考勤记录** 表
4. 点击底部 **「+ 添加一行」**
5. 填：姓名、日期、签到时间、打卡类型（选外勤）、外勤地点
6. 保存。下班时再回到这一行，补充签退时间，工作时长自动出来

> 💡 提示：把「考勤记录」表设置一个 **仅显示自己的打卡记录** 的筛选视图，避免看到全公司的数据。

### 6.2 手机端录入项目收支

同样的方式，切到「项目收支明细」表，点「+」新增，选关联项目、填金额、选类型，如果拍了发票照片还可以上传附件。

### 6.3 把多维表格添加到手机桌面（快捷入口）

WPS App → 在多维表格文件名右侧点 **「···」** → **「添加到桌面」**，之后像 App 一样一键打开。

---

## 第七步：数据装填顺序（重要！）

建好结构后，**按这个顺序填数据**，不要乱：

1. **先填项目总表** → 把所有现有项目录进去（项目名、报价、状态）
2. **再填历史收支** → 在「项目收支明细」里把过去的成本和收入补录进去
3. **日常打卡** → 每天在「考勤记录」打卡
4. **仪表盘刷新** → 仪表盘会自动拉最新数据，无需手动更新

---

## 常见问题

### Q1：工作时长公式报错怎么办？
A：检查签到/签退字段是不是「日期时间」类型（不是纯「日期」也不是纯「文本」）。如果还是不行，把公式换成最简单的 `{签退时间} - {签到时间}`，手动 × 24 心算也行。

### Q2：关联字段找不到项目总表？
A：确保「项目总表」已经创建好了，且两张表在同一个多维表格文件里（左下角的表列表能看到两张表）。

### Q3：手机端字段显示不全？
A：在电脑端编辑视图时，把常用字段拖到前面，不常用的可以隐藏（字段右键 → 隐藏）。手机端会按视图显示。

### Q4：仪表盘数据不更新？
A：多维表格是实时计算的，不用手动刷新。如果没变化，检查筛选条件是不是设错了（比如日期范围）。

### Q5：能不能导出成 Excel？
A：可以。多维表格右上角 **「···」** → **「导出」** → 选 Excel 格式。但导出后关联关系和公式会丢失，只保留数据。

---

## 快速检查清单

建成后自查以下项目：

- [ ] 考勤记录表：能正常添加打卡记录，工作时长公式自动计算
- [ ] 项目总表：已有全部项目，看板视图能拖拽改状态
- [ ] 项目收支明细：关联字段正常工作，能选到项目；按月分组视图正常
- [ ] 仪表盘：统计数字、柱状图、折线图都有数据
- [ ] 手机端：三张表都能打开、能新增记录、打卡流程顺畅
- [ ] 分享：如需给同事用，点右上角「分享」设置权限

---

> **下一步**：打开 WPS，对照本文从第一步开始操作。遇到具体报错随时问我。
